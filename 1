#include <iostream>
using namespace std;
int main ()
{
   int n, m, i, j, t;
   cin >> n >> m;
   int a[n];
   int b[m];
   for (i = 0; i < n; i++)
    {
      cin >> a[i];
    }
   for (j=0; j<m; j++)
    {
        cin>>b[j];
    }
    for (i=0; i<n; i++)
    {
        for(j=0; j<m; j++)
        {
           t=a[i];
           a[i]=b[j];
           b[j]=t;
        }
    }
    for (i=0; i<n; i++)
    {
        cout<<a[i]<<" ";
    }
    cout<<endl;
    for(j=0; j<m; j++)
        {
           cout<<b[j]<<" ";
        }

  return 0;
}
